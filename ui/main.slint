// Aggregator compiled by build.rs; re-exports the two top-level components so
// that `slint::include_modules!()` exposes both `Overlay` and `Settings`.

export { Overlay } from "overlay.slint";
export { Settings } from "settings.slint";
